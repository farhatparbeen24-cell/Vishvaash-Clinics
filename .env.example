# Set this once the final production domain is live (no trailing slash needed).
# It becomes the single source for canonical URL, sitemap.xml, OG url and JSON-LD.
# Leave unset while on the *.vercel.app preview domain to keep the placeholder.
# NEXT_PUBLIC_SITE_URL=https://www.vishvaasclinics.com
